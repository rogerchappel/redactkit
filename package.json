{
  "name": "@rogerchappel/redactkit",
  "version": "0.1.0",
  "description": "Local-first CLI for scrubbing secrets and private details from logs, transcripts, and fixtures.",
  "type": "module",
  "bin": {
    "redactkit": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "dev": "tsx src/cli.ts",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test dist/tests/*.test.js",
    "smoke": "pnpm run build && bash scripts/smoke.sh",
    "package:smoke": "npm pack --dry-run",
    "release:check": "pnpm run check && pnpm test && pnpm run smoke && npm run package:smoke"
  },
  "keywords": [
    "redaction",
    "secrets",
    "privacy",
    "logs",
    "cli"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
